// creating a custom math module
exports.square = (a) => (a ** a)

// exports.PI = () => { return math.PI; }
// const add = (a, b) => (a + b)
// const sub = (a, b) => (a - b)
// const mul = (a, b) => (a * b)
// const div = (a, b) => (a / b)

// module.exports = { add, sub, mul, div }



